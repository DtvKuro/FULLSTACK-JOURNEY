// var generateName = require('./generateName.js');

import generateName from 'sillyname';
var sillyName = generateName();

//randomo supe generator

import { randomSuperhero as generator } from 'superheroes';

var heroName = generator();
console.log(`I am  ${heroName}.`);